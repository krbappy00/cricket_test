/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "www.worldometers.info",
            "cdn.britannica.com"
            
        ],
    },
}

module.exports = nextConfig
